<%- include('partials/header') %>
<main class="container-fluid p-0 vh-100">
    <div class="row g-0 h-100">
        <div class="col-md-6 col-lg-7 d-none d-md-block position-relative" style="background: linear-gradient(135deg, #0d9488 0%, #042f2e 100%);">
            <div class="position-absolute top-50 start-50 translate-middle w-100 text-center text-white px-4">
                <h1 class="display-3 fw-bold mb-3 text-shadow">Security Verification</h1>
                <p class="fs-4 fw-light text-shadow">Answer your security question.</p>
            </div>
            <a href="/login" class="position-absolute top-0 start-0 m-4 text-white text-decoration-none fw-semibold fs-5 text-shadow">
                &larr; Back to Login
            </a>
        </div>
        <div class="col-md-6 col-lg-5 bg-white d-flex align-items-center justify-content-center px-4 py-5">
            <div class="w-100" style="max-width: 450px;">
                <a href="/login" class="d-md-none text-teal text-decoration-none fw-semibold mb-4 d-inline-block">
                    &larr; Back to Login
                </a>
                <h2 class="text-center mb-5 fw-bold" style="color: var(--primary-teal);">Security Verification</h2>
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger text-center mb-4 shadow-sm" role="alert">
                        <%= error %>
                    </div>
                <% } %>
                <form action="/forgot-password/verify" method="POST">
                    <input type="hidden" name="username" value="<%= user.username %>">
                    <div class="mb-4">
                        <label class="form-label text-secondary fw-semibold">Security Question</label>
                        <p class="form-control form-control-lg bg-light border-0 shadow-none"><%= user.security_question || "City you were born in?" %></p>
                    </div>
                    <div class="mb-4">
                        <label for="securityAnswer" class="form-label text-secondary fw-semibold">Security Answer</label>
                        <input type="text" class="form-control form-control-lg bg-light border-0 shadow-none" id="securityAnswer" name="securityAnswer" required placeholder="Enter your answer">
                    </div>
                    <button type="submit" class="btn btn-primary btn-lg w-100 rounded-pill fw-bold shadow-sm mb-3">Verify Answer</button>
                </form>
            </div>
        </div>
    </div>
</main>
</body>
</html>
